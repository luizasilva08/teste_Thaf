Aguardando o novo schema do banco.

Cada tabela vira um arquivo aqui, no formato `<numero>_<tabela>.sql`
(o número garante a ordem de criação, respeitando as FOREIGN KEY — veja
`../criar_tabelas.py` e `../../../NOTION_TEMPLATE_CLASSE.md`).
